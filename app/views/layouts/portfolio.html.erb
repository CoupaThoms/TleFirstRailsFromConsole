<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <title><%= @page_title %></title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>


    <meta name="keywords" content="<%= @seo_keywords %>" />
    <%= favicon_link_tag 'favicon/favicon.ico' %>

    <%= stylesheet_link_tag    'portfolios', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

    <%= render 'shared/portfolio_nav' %> <%# we can just say shared/portfolio_nav. Il sait que c'est dans view et il sait que c'est _ devant car c'est un partial %>

    <main role="main">

      <section class="jumbotron text-center">
        <div class="container">
          <h1 class="jumbotron-heading">Mon portfolio</h1>
          <p class="lead text-muted">Voila yeah :)</p>
          <p>
            <%= link_to 'Find out more about me', about_thomas_path, class: 'btn btn-primary' %>
            <%= link_to 'My Blog', blogs_path, class: 'btn btn-secondary' %>
          </p>
        </div>
      </section>

    </main>

    <%= yield %>

    <%= alerts %>

    <%= render 'shared/portfolio_footer' %>

    <%= source_helper("portfolio") %>

  </body>
</html>
