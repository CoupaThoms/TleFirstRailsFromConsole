<!DOCTYPE html>
<html>
  <head>
    <title><%= @page_title %></title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta name="keywords" content="<%= @seo_keywords %>" />

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <%= render "shared/nav", location: 'top' %> <%# we can just say shared/nav. Il sait que c'est dans view et il sait que c'est _ devant car c'est un partial %>

    <%= login_helper %> <%# Celui la est un helper. Pas un partial car on a de la logique dedans donc on a fait un helper %>

    <%= yield %>

    <% if session[:getVarInSession] %>
    <br/><br/><br/>
    <p>Thanks for visiting me from  <%= session[:getVarInSession] %></p>
    <% end %>

  </body>
</html>
