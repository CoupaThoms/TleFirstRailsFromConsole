<%= form_for(blog) do |form| %>
  <% if blog.errors.any? %>

    <%= blog.errors.full_messages.each do | err | %>
      <%= js add_gritter(err, title: "Thomas Lefèvre Blog", sticky: false) %>
    <% end %>

    <div id="error_explanation">
      <p>La version error sans cocoon: </p>
      <h2><%= pluralize(blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.text_field :title, class: 'form-control', placeholder: 'Title' %>
  </div>

  <div class="form-group">
    <%= form.text_area :body, class: 'form-control', rows: 15, placeholder: 'Body' %>
  </div>

  <div class="form-group">
    <%= form.submit "Submit", class: 'btn btn-primary' %>
  </div>
<% end %>
